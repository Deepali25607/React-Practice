import React from 'react';
//import App from '../../src/App';
const Contact =() =>{
  return(
       <p>At first glance, Broker Notes‘ contact page looks pretty bare.

       There’s no graphics, no quirky copy, just a plain old contact form.
       
       Great for UX, but not so great for inspiring users to reach out.</p>
  );  
}
export default Contact;